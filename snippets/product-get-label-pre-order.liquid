<div class="label label--pre-order mb-3 mr-3 text-nowrap{% if variant_pre_order != true %} d-none-important{% endif %}" data-js-product-label-pre-order>{% if available %}{{ 'products.product.label.pre_order' | t }}{% else %}Pre-order Limit Reached{% endif %}</div>
{%- if variant_pre_order -%}
    {%- assign has_labels = true -%}
{%- endif -%}